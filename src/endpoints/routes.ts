const ROOT = '/';
const API = '/api';
const V1 = '/v1';

//PILET
const PILET = '/pilet';

const PILETS = '/pilets';
const VERSIONS = '/versions';
const TRIGGER = '/trigger';

export { ROOT, API, V1, PILET, PILETS, VERSIONS, TRIGGER };
