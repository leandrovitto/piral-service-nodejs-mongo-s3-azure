const ROOT = '/';
const API = '/api';
const V1 = '/v1';

//PILET
const PILET = '/pilet';

const PILETS = '/pilets';
const VERSIONS = '/versions';
const TRIGGER = '/trigger';

const ADMIN = '/admin';
const LOGIN = '/login';
const LOGOUT = '/logout';

export {
  ROOT,
  ADMIN,
  API,
  V1,
  PILET,
  PILETS,
  VERSIONS,
  TRIGGER,
  LOGIN,
  LOGOUT,
};
